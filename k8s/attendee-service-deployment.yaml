apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendee-service
  labels:
    app: attendee-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: attendee-service
  template:
    metadata:
      labels:
        app: attendee-service
    spec:
      containers:
        - name: attendee-service
          image: conference/attendee-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "default"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: attendee-service
spec:
  selector:
    app: attendee-service
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
